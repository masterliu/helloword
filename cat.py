# -*- coding: UTF-8 -*- import sysdef readfile(filename):	'''print a file to the standard output.'''	f = file(filename)	while True:		line = f.readline()		if len(line) == 0:			break		print line	f.close()	if len(sys.argv)<2:	print 'no action specified.'	sys.exit()	if sys.argv[1].startswith('-'):	option = sys.argv[1][2:]	if option =='version':		print'version 1.2'	elif option =='help':		print'''\		this program prints files to the standard output		any number of file scan be specified.		-version:prints the version		-help: display this help'''	else:		print'unknow option.'	sys.exit()	else:	for filename in sys.argv[1:]:		readfile(filename)